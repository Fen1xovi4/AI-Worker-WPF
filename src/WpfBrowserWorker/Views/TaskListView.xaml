<UserControl x:Class="WpfBrowserWorker.Views.TaskListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="{StaticResource ColorBackground}">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Active Tasks" FontSize="22" FontWeight="SemiBold"
                   FontFamily="Segoe UI" Foreground="{StaticResource ColorTextPrimary}"
                   Margin="0,0,0,16"/>

        <Border Grid.Row="1" Background="{StaticResource ColorSurface}"
                CornerRadius="6" BorderBrush="{StaticResource ColorBorder}" BorderThickness="1">
            <ListView ItemsSource="{Binding ActiveTasks}"
                      BorderThickness="0" Background="Transparent"
                      ScrollViewer.HorizontalScrollBarVisibility="Auto">
                <ListView.View>
                    <GridView>
                        <GridViewColumn Header="" Width="36"
                                        DisplayMemberBinding="{Binding StatusIcon}"/>
                        <GridViewColumn Header="Type" Width="130"
                                        DisplayMemberBinding="{Binding TaskType}"/>
                        <GridViewColumn Header="Account" Width="170"
                                        DisplayMemberBinding="{Binding AccountId}"/>
                        <GridViewColumn Header="Status" Width="110"
                                        DisplayMemberBinding="{Binding Status}"/>
                        <GridViewColumn Header="Duration" Width="90"
                                        DisplayMemberBinding="{Binding Duration}"/>
                    </GridView>
                </ListView.View>
            </ListView>
        </Border>
    </Grid>
</UserControl>
